# Contribuir

1. Crea una rama `feature/*` desde `main`.
2. Mantén los módulos tipados y valida entradas con Zod.
3. Ejecuta `npm run type-check`, `npm run lint` y `npm test` antes de abrir un PR.
4. Describe el cambio y sus riesgos en la solicitud.
